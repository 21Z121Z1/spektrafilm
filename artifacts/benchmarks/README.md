# Runtime Benchmarks

JSON files in this directory are generated by:

```bash
uv run python scripts/benchmark_runtime_backend.py --backend cpu --sizes 512 1024
```

Use `--backend auto` or `--backend mlx` to exercise backend selection on machines
with the optional Apple GPU dependency installed.
